[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = [
  "pip install uv",
  "uv sync"
]

[phases.build]
cmds = []

[start]
cmd = "uv run uvicorn app:app --host 0.0.0.0 --port $PORT"

